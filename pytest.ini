[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: end-to-end tests that launch a real browser
filterwarnings =
    ignore::DeprecationWarning
